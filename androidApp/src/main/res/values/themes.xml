<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws 100% of the UI; the platform theme just needs to get
         out of the way (no action bar, no title). -->
    <style name="Theme.DummyTvApp" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
    </style>
</resources>
